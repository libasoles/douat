const initialState = {
  tiles: [[]],
  zoomLevel: 1
};

export default function canvas(state = initialState, action) {
  switch (action.type) {
    case "":
      break;
    default:
      return state;
  }
}
